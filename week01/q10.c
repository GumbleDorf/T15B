#include<stdio.h>
#include<stdlib.h>
int main(void) {


}

void isVowel(char ch) {
    assert(islower(ch));
    (ch == 'a' || ch == 'i' ...) ? printf("vowel") : printf("consonant");
    if(ch == 'a' || ch == 'i' ...) {
        printf("vowel");
    } else {
        printf("consonant");
    }
}